[pytest]
markers =
    unit: fast isolated unit tests
    integration: API and workflow integration tests
    slow: longer-running tests such as full benchmark
testpaths = tests
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
